<div class="container py-4">
  <div class="card mb-4">
    <div class="card-body">
      <div class="d-flex justify-content-between align-items-center">
        <h1 class="h3 mb-0">Nouvelle chambre</h1>
        <%= link_to "Retour", admin_rooms_path, class: "btn btn-link" %>
      </div>
    </div>
  </div>

  <div class="card">
    <div class="card-body">
      <%= render "form",
        room: @room,
        submit_label: "Créer la chambre",
        back_label: "Retour",
        back_path: admin_rooms_path %>
    </div>
  </div>
</div>
