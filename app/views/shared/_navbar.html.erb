<nav class="navbar navbar-expand-sm navbar-light navbar-lewagon sticky-top" data-controller="sticky-header">
  <div class="container-fluid">
    <%= link_to root_path, class: "navbar-brand" do %>
      <i class="fa fa-home" aria-hidden="true"></i>
    <% end %>

    <% if owner_signed_in? %>
      <ul class="navbar-nav d-none d-sm-flex flex-row align-items-center gap-2 ms-2">
        <li class="nav-item">
          <%= link_to t("shared.nav.dashboard"), admin_bookings_path, class: ["nav-link", (request.path.start_with?(admin_bookings_path) ? "active" : nil)].compact.join(" ") %>
        </li>
        <li class="nav-item">
          <%= link_to admin_inbox_path, class: ["nav-link", (request.path == admin_inbox_path ? "active" : nil)].compact.join(" ") do %>
            <%= t("shared.nav.inbox") %>
            <% if @owner_unread_conversations_count.to_i.positive? %>
              <span class="badge text-bg-warning"><%= @owner_unread_conversations_count %></span>
            <% end %>
          <% end %>
        </li>
        <li class="nav-item">
          <%= link_to t("shared.nav.planning"), admin_rooms_path, class: ["nav-link", (request.path.start_with?(admin_rooms_path) ? "active" : nil)].compact.join(" ") %>
        </li>
        <li class="nav-item">
          <%= link_to t("shared.nav.clients"), admin_clients_path, class: ["nav-link", (request.path.start_with?(admin_clients_path) ? "active" : nil)].compact.join(" ") %>
        </li>
      </ul>
    <% end %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <% current_path = request.path %>
      <% if owner_signed_in? %>
        <ul class="navbar-nav d-sm-none gap-2">
          <li class="nav-item">
            <%= link_to t("shared.nav.dashboard"), admin_bookings_path, class: ["nav-link", (current_path.start_with?(admin_bookings_path) ? "active" : nil)].compact.join(" ") %>
          </li>
          <li class="nav-item">
            <%= link_to admin_inbox_path, class: ["nav-link", (current_path == admin_inbox_path ? "active" : nil)].compact.join(" ") do %>
              <%= t("shared.nav.inbox") %>
              <% if @owner_unread_conversations_count.to_i.positive? %>
                <span class="badge text-bg-warning"><%= @owner_unread_conversations_count %></span>
              <% end %>
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to t("shared.nav.planning"), admin_rooms_path, class: ["nav-link", (current_path.start_with?(admin_rooms_path) ? "active" : nil)].compact.join(" ") %>
          </li>
          <li class="nav-item">
            <%= link_to t("shared.nav.clients"), admin_clients_path, class: ["nav-link", (current_path.start_with?(admin_clients_path) ? "active" : nil)].compact.join(" ") %>
          </li>
        </ul>

        <ul class="navbar-nav me-auto gap-2">
          <li class="nav-item">
            <%= link_to t("shared.nav.rooms"), rooms_path, class: ["nav-link", (current_path == rooms_path ? "active" : nil)].compact.join(" ") %>
          </li>

          <li class="nav-item">
            <%= link_to t("shared.nav.blog"), articles_path, class: ["nav-link", (current_path.start_with?(articles_path) ? "active" : nil)].compact.join(" ") %>
          </li>
        </ul>
      <% else %>
        <ul class="navbar-nav me-auto gap-2">
          <li class="nav-item">
            <%= link_to t("shared.nav.rooms"), rooms_path, class: ["nav-link", (current_path == rooms_path ? "active" : nil)].compact.join(" ") %>
          </li>

          <li class="nav-item">
            <%= link_to t("shared.nav.blog"), articles_path, class: ["nav-link", (current_path.start_with?(articles_path) ? "active" : nil)].compact.join(" ") %>
          </li>

          <% if user_signed_in? %>
            <li class="nav-item">
              <%= link_to t("shared.nav.my_bookings"), bookings_path, class: ["nav-link", (current_path.start_with?(bookings_path) ? "active" : nil)].compact.join(" ") %>
            </li>
            <li class="nav-item">
              <%= link_to inbox_path, class: ["nav-link", (current_path == inbox_path ? "active" : nil)].compact.join(" ") do %>
                <%= t("shared.nav.inbox") %>
                <% if @user_unread_conversations_count.to_i.positive? %>
                  <span class="badge text-bg-warning"><%= @user_unread_conversations_count %></span>
                <% end %>
              <% end %>
            </li>
          <% end %>
        </ul>
      <% end %>

      <ul class="navbar-nav ms-auto gap-2">
        <% if (owner_signed_in? || user_signed_in?) && @login_notifications_count.to_i.positive? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle d-inline-flex align-items-center position-relative" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false" aria-label="<%= t('notifications.login.title') %>">
              <i class="fa-solid fa-bell" aria-hidden="true"></i>
              <span class="position-absolute top-0 start-100 translate-middle-y badge rounded-pill text-bg-danger">
                <%= @login_notifications_count %>
              </span>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" style="min-width: 320px;">
              <li class="dropdown-header"><%= t("notifications.login.title") %></li>
              <% @login_notifications.each do |notification| %>
                <li class="px-3 py-2">
                  <div class="small"><%= notification["text"] || notification[:text] %></div>
                  <% url = notification["url"] || notification[:url] %>
                  <% if url.present? %>
                    <%= link_to(
                      (notification["action_label"] || notification[:action_label] || t("notifications.login.actions.open")),
                      mark_notifications_read_path(redirect_to: url),
                      class: "btn btn-sm btn-outline-primary mt-2"
                    ) %>
                  <% end %>
                </li>
                <li><hr class="dropdown-divider"></li>
              <% end %>
            </ul>
          </li>
        <% end %>

        <li class="nav-item d-flex flex-column">
          <%= link_to switch_locale_path(nil), class: ["nav-link", "py-0", "d-inline-flex", "align-items-center", "gap-1", (I18n.locale == :fr ? "active" : nil)].compact.join(" "), aria: { label: "Français" } do %>
            <%= image_tag "flags/fr.svg", class: "lv-locale-flag", alt: "FR", width: 18, height: 12, loading: "lazy" %>
            <span class="text-uppercase" style="font-size: .7rem; letter-spacing: .04em;">FR</span>
          <% end %>

          <%= link_to switch_locale_path(:en), class: ["nav-link", "py-0", "d-inline-flex", "align-items-center", "gap-1", (I18n.locale == :en ? "active" : nil)].compact.join(" "), aria: { label: "English" } do %>
            <%= image_tag "flags/en.svg", class: "lv-locale-flag", alt: "EN", width: 18, height: 12, loading: "lazy" %>
            <span class="text-uppercase" style="font-size: .7rem; letter-spacing: .04em;">EN</span>
          <% end %>
        </li>

        <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle d-inline-flex align-items-center gap-2" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false" aria-label="<%= t('shared.nav.account') %>">
              <i class="fa-solid fa-circle-user" aria-hidden="true"></i>
              <span class="d-none d-sm-inline"><%= current_user.display_name %></span>
            </a>
            <ul class="dropdown-menu dropdown-menu-end">
              <li>
                <span class="dropdown-item-text fw-semibold"><%= current_user.display_name %></span>
              </li>
              <li><hr class="dropdown-divider"></li>
              <li>
                <%= link_to t("shared.nav.edit_profile"), edit_profile_path, class: "dropdown-item" %>
              </li>
              <li>
                <%= link_to t("shared.nav.logout"), logout_path, data: { turbo_method: :delete }, class: "dropdown-item" %>
              </li>
            </ul>
          </li>
        <% elsif owner_signed_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle d-inline-flex align-items-center gap-2" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false" aria-label="<%= t('shared.nav.account') %>">
              <i class="fa-solid fa-circle-user" aria-hidden="true"></i>
              <span class="d-none d-sm-inline"><%= current_owner.display_name %></span>
            </a>
            <ul class="dropdown-menu dropdown-menu-end">
              <li>
                <span class="dropdown-item-text fw-semibold"><%= current_owner.display_name %></span>
              </li>
              <li><hr class="dropdown-divider"></li>
              <li>
                <%= link_to t("shared.nav.edit_profile"), edit_profile_path, class: "dropdown-item" %>
              </li>
              <li>
                <%= link_to t("shared.nav.logout"), logout_path, data: { turbo_method: :delete }, class: "dropdown-item" %>
              </li>
            </ul>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to login_path(return_to: request.fullpath), class: "nav-link", data: { turbo: false }, aria: { label: t("shared.nav.login") } do %>
              <i class="fa fa-user-plus" aria-hidden="true"></i>
              <span class="ms-1 d-none d-sm-inline"><%= t("shared.nav.login") %></span>
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
